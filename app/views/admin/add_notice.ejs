<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Formulário de cadastro de notícia</title>
    <link rel="shortcut icon" href="images/favicon.ico"></link>
    <link href="bootstrap/css/bootstrap.min.css" rel="stylesheet">
  </head>
  <body>
    <nav class="navbar navbar-expand-lg bg-body-tertiary mb-3">
      <div class="container">
        <a class="navbar-brand" href="/">Dashboard</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link" href="notices">Notícias</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="notice_add">Adicionar notícia</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container">
      <h1 class="mb-5">Adicionar notícia</h1>
      <hr>

      <% if(errors) { %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
          <ul>
            <% for(var i = 0; i < errors.length; i++) { %>
              <li><%= errors[i].msg %></></li>
            <% } %>
          </ul>
        </div>
      <% } %>

      <form action="/notice_save" method="post">
        <div class="mb-3">
          <label class="form-label">Título:</label>
          <input type="text" name="title" id="title" class="form-control" value="<%= body?.title %>">
        </div>

        <div class="mb-3">
          <label class="form-label">Resumo:</label>
          <input type="text" name="summary" id="summary" class="form-control" value="<%= body?.summary %>">
        </div>

        <div class="mb-3">
          <label class="form-label">Descrição:</label>
          <textarea id="description" name="description" class="form-control" rows="5" cols="30"><%= body?.description %></textarea>
        </div>

        <div class="mb-3">
          <label class="form-label">Autor:</label>
          <input type="text" name="author" id="author" class="form-control" value="<%= body?.author %>">
        </div>

        <div class="mb-3">
          <label class="form-label">Data da notícia:</label>
          <input type="date" name="date_notice" id="date_notice" class="form-control" value="<%= body?.date_notice %>">
        </div>
        
        <button type="submit" class="btn btn-primary">Enviar</button>
      </form>
    </div>
    
    <script src="bootstrap/js/bootstrap.bundle.min.js"></script>
  </body>
</html>